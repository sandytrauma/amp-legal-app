import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "AMP Legal Solutions Pvt. Ltd.",
  description: "AMP Legal Solutions Pvt. Ltd. is a leading legal services provider, offering comprehensive solutions to individuals and businesses. With a team of experienced professionals, we specialize in various areas of law, including corporate law, intellectual property, litigation, and more. Our commitment to excellence and client satisfaction sets us apart in the legal industry.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
